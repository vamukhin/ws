let uuidv4;_25d‍.x([["fakeDataBase",()=>fakeDataBase]]);_25d‍.w("uuid/v4",[["default",["uuidv4"],function(v){uuidv4=v}]]);

class FakeDataBase {
    constructor(){
        this.messages = [];
        this.users = [];
    }

    addId(obj){
        return {
            ...obj,
            id: uuidv4()
        }
    }

    addNewMessage(message){
        const messageWithId = this.addId(message);
        this.messages.push(messageWithId);
        return messageWithId;
    }

    addNewUser(user){
        const userWithId = this.addId(user);
        this.users.push(userWithId);
        return userWithId.id;
    }

}

       const fakeDataBase = new FakeDataBase();