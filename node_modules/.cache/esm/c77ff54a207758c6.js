let fakeDataBase;_207‍.x([["onFakeAuthHandler",()=>onFakeAuthHandler]]);_207‍.w("../../../fake-data-base",[["fakeDataBase",["fakeDataBase"],function(v){fakeDataBase=v}]]);

       function onFakeAuthHandler(payload) {
    const {webSocket, type} = this;
    const {name} = payload;
    const newUserId = fakeDataBase.addNewUser({name, webSocket});
    _207‍.g.console.log(
        fakeDataBase.users.map(us => `${us.id} + ${us.name}`)
    )
    const data = JSON.stringify({
        type,
        payload: {id: newUserId}
    })
    webSocket.send(data)
}